spring:
  datasource:
    url: jdbc:tc:postgresql:///testdatabase
    username: testuser
    password: testpassword
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
  flyway:
    enabled: false